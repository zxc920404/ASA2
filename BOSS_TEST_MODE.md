# Boss 測試模式設定

## ⚠️ 當前狀態：測試模式已啟用

目前 Boss 生成時間已調整為**測試模式**，方便快速測試三當家的奔跑動畫和技能。

### 📅 生成時間對照表

| Boss | 正式版 | 測試模式 | 變更 |
|------|--------|---------|------|
| 三當家（elite_shield） | 2:30（150 秒） | **0:10（10 秒）** | ⚡ 快 15 倍 |
| 二當家（elite_shooter） | 5:00（300 秒） | **0:20（20 秒）** | ⚡ 快 15 倍 |
| 大當家（elite_charger） | 7:30（450 秒） | **0:30（30 秒）** | ⚡ 快 15 倍 |

### 🎯 測試模式優點

1. **快速驗證**：10 秒即可看到三當家奔跑動畫
2. **連續測試**：30 秒內可測試所有三個 Boss
3. **技能檢查**：快速驗證技能移除是否正確
4. **動畫測試**：快速驗證三當家 Sprite 動畫是否正常播放

### 🎮 測試流程建議

#### 測試一：三當家動畫驗證
1. 啟動遊戲，選擇任意宗門
2. 進入遊戲後等待 **10 秒**
3. 觀察三當家出現時是否顯示動畫（而非 Graphics 圖形）
4. 確認動畫流暢播放（10 fps，8 幀循環）
5. 確認尺寸正確（64×64，比普通小怪大）

#### 測試二：技能移除驗證
1. **10 秒**：三當家出現
   - ❌ 不應觸發震罡功（護盾蓄力技能）
   - ❌ 不應出現護盾圓形視覺
   - ✅ 只使用霸山墜、震撼咆哮、連續重擊
2. **20 秒**：二當家出現
   - ❌ 不應生成黑洞
   - ✅ 只使用彈幕模式和外圍直線射擊
3. **30 秒**：大當家出現
   - ❌ 靠近時不應出現前搖預警扇形
   - ❌ 不應觸發霸刀橫斬
   - ✅ 只使用三個大招

#### 測試三：完整戰鬥流程
1. 依序擊敗三個 Boss（10/20/30 秒出現）
2. 確認 Boss 死亡後正常掉落經驗球
3. 確認無 console 錯誤或警告
4. 確認遊戲流程正常（可繼續遊玩至 10 分鐘勝利）

### 🔧 修改位置

**檔案：** `src/scenes/GameScene.ts`

**修改行數：** 約 773-785 行

```typescript
// ── 精英怪事件觸發（測試模式：10 / 20 / 30 秒；正式版：150 / 300 / 450 秒）──
if (!this.eliteSpawned150 && this.elapsedSeconds >= 10) {  // 測試：10 秒
  this.eliteSpawned150 = true;
  this.spawnEliteEnemy(1);
}
if (!this.eliteSpawned300 && this.elapsedSeconds >= 20) {  // 測試：20 秒
  this.eliteSpawned300 = true;
  this.spawnEliteEnemy(2);
}
if (!this.eliteSpawned450 && this.elapsedSeconds >= 30) {  // 測試：30 秒
  this.eliteSpawned450 = true;
  this.spawnEliteEnemy(3);
}
```

### 🔄 切換回正式版

測試完成後，如需切換回正式版時間，修改以下數值：

```typescript
// 正式版設定
if (!this.eliteSpawned150 && this.elapsedSeconds >= 150) {  // 2:30
if (!this.eliteSpawned300 && this.elapsedSeconds >= 300) {  // 5:00
if (!this.eliteSpawned450 && this.elapsedSeconds >= 450) {  // 7:30
```

並更新註解：
```typescript
// ── 精英怪事件觸發（正式版：150 / 300 / 450 秒，即 2:30 / 5:00 / 7:30）──
```

### 📊 建置結果

✅ **建置成功**
- 46 modules transformed
- Bundle 大小：232.71 kB（無變化）
- 建置時間：6.25 秒
- 無 TypeScript 錯誤
- 無 ESLint 警告

### 📝 注意事項

1. **難度倍率正常**：測試模式只改變生成時間，不影響難度倍率計算
2. **勝利時間不變**：遊戲仍需 10 分鐘才觸發勝利（VICTORY_TIME_MS）
3. **經驗掉落正常**：Boss 死亡仍正常掉落經驗球
4. **技能冷卻正常**：Boss 技能冷卻時間不受測試模式影響

### 🚀 快速測試指令

啟動開發伺服器（需手動執行）：
```bash
npm run dev
```

建置生產版本：
```bash
npm run build
```

### 🎯 測試檢查清單

#### 三當家（10 秒出現）
- [ ] 顯示動畫（而非 Graphics 圖形）
- [ ] 動畫流暢播放（10 fps）
- [ ] 尺寸正確（64×64）
- [ ] 不出現護盾圓形
- [ ] 受擊時無減傷（造成完整傷害）
- [ ] 只使用三個技能（霸山墜、震撼咆哮、連續重擊）

#### 二當家（20 秒出現）
- [ ] 不生成黑洞
- [ ] 玩家不被吸引
- [ ] 彈幕模式正常（1.7 秒輪替）
- [ ] Line Attack 正常（6～8 秒冷卻）

#### 大當家（30 秒出現）
- [ ] 靠近時不出現前搖預警
- [ ] 不觸發霸刀橫斬
- [ ] 只使用三個大招
- [ ] 沒有技能時只追蹤移動

#### 通用檢查
- [ ] 無 console 錯誤
- [ ] Boss 死亡正常掉落經驗球
- [ ] 遊戲流程正常（可繼續至勝利）

---

**設定時間**：2025-01-XX  
**設定者**：Kiro AI Assistant  
**狀態**：測試模式已啟用  
**版本**：1.0
